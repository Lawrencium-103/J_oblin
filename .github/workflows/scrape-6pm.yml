name: Scrape - 6 PM

on:
  schedule:
    - cron: "0 17 * * *"
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger scrape on Render
        run: |
          curl -s -X POST "https://joblin-hx5a.onrender.com/api/scrape/cron?token=${{ secrets.JOBLIN_CRON_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d '{"title": "Data Analyst", "boards": []}' \
            --max-time 300
